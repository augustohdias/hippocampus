{
  "name": "hippocampus-cc-plugin",
  "version": "0.0.1",
  "description": "Claude Code plugin for Hippocampus memory system",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rm -rf plugin/scripts/*.cjs",
    "dev": "node scripts/build.js --watch"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
